//- Extends from layout.pug and overrides the black named content
extends layout

block content
  h1(class='title') Home Library
  p Welcome to your #[em HomeLibrary], the easy way to manage your books...

  h1 Library Records

  //- Checks if the 'error' variable passed in from the render() function has been defined
  if error
    p Error getting dynamic content.
  else
    p Your library has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Copies:] !{data.book_instance_count}
      li #[strong Copies available:] !{data.book_instance_available_count}
      li #[strong Authors:] !{data.author_count}
      li #[strong Genres:] !{data.genre_count}
